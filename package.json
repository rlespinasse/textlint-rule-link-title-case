{
  "name": "textlint-rule-link-title-case",
  "version": "0.0.0",
  "keywords": [
    "textlint",
    "textlintrule",
    "links",
    "title-case",
    "ap-style",
    "markdown"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/",
    "src/"
  ],
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build",
    "audit": "npm audit"
  },
  "peerDependencies": {
    "textlint": ">= 14.5.0"
  },
  "devDependencies": {
    "@textlint/types": "^14.5.0",
    "textlint-scripts": "^14.5.0",
    "textlint-tester": "^14.5.0"
  }
}
